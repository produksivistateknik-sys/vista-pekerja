# -*- coding: utf-8 -*-
"""
Script: apply_mulai_simpan_paling_atas.py
Tujuan: Susun ulang LAGI - kali ini [Mulai]+[Simpan Progress] jadi elemen
        PALING ATAS di tiap kartu (sebelum header nama komponen/info QTY),
        biar posisinya nempel persis di bawah banner "Pilih Operator".

PRASYARAT: apply_reorder_kartu.py sudah pernah dijalankan.

Cara pakai:
    python apply_mulai_simpan_paling_atas.py
"""
import shutil
import datetime
import sys

FILE_PATH = r"C:\Users\User\vista-pekerja\src\App.tsx"

def fail(msg):
    print("\n❌ GAGAL:", msg)
    print("Tidak ada perubahan yang disimpan. File asli aman.")
    sys.exit(1)

def main():
    try:
        with open(FILE_PATH, "r", encoding="utf-8") as f:
            content = f.read()
    except FileNotFoundError:
        fail(f"File tidak ditemukan di {FILE_PATH}")

    old = '                    <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",gap:8}}>\n                      <div style={{display:"flex",flexDirection:"column",gap:3}}>\n                        <div style={{display:"flex",alignItems:"center",gap:6,flexWrap:"wrap"}}>\n                          {r.wpDef&&<span style={{background:r.wpDef.color+"18",color:r.wpDef.color,border:`1px solid ${r.wpDef.color}33`,borderRadius:4,padding:"1px 6px",fontSize:10,fontWeight:700}}>{r.wpDef.wp}</span>}\n                          <span style={{fontWeight:700,fontSize:13,color:"#374151"}}>{r.item.nama}</span>\n                        </div>\n                        <div style={{fontSize:10,color:"#94a3b8"}}>{r.task.proyek} · {r.panel.nama}</div>\n                        <div style={{display:"flex",alignItems:"center",gap:6,flexWrap:"wrap"}}>\n                          <span style={{fontSize:10,color:"#94a3b8",fontFamily:"\'DM Mono\',monospace"}}>{r.kode}</span>\n                          <Badge label={r.task.prioritas||"Sedang"} color={r.priColor}/>\n                          {cardMode===\'timer\'&&r.wiringBadge&&(\n                            <span style={{background:"#eef2ff",color:"#4f46e5",borderRadius:6,padding:"1px 6px",fontSize:9,fontWeight:700}}>\n                              ⚡ {(r.wiringBadge.bobot||"").replace("_"," ")} · {r.wiringBadge.jumlahOrang||"–"}org\n                            </span>\n                          )}\n                        </div>\n                      </div>\n                      {r.pct===100\n                        ?<Badge label="TERCAPAI" color="#16a34a" bg="#dcfce7"/>\n                        :r.pct===0\n                        ?<Badge label="BELUM MULAI" color="#94a3b8" bg="#f1f5f9"/>\n                        :<Badge label="ON PROGRESS" color="#2563eb" bg="#dbeafe"/>\n                      }\n                    </div>\n\n                    <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:5,fontSize:10,color:"#64748b"}}>\n                      <div>QTY KOMP: <b style={{color:"#475569"}}>{r.qtyKomp} 🔒</b></div>\n                      <div>CREATE BY: <b style={{color:"#475569"}}>{kInfo.createdBy||"–"}</b></div>\n                      <div>TARGET: <b style={{color:"#1d4ed8"}}>{fmtD(kInfo.targetSelesai)}</b></div>\n                      <div>AKTUAL: <b style={{color:r.pct>=100?"#16a34a":"#94a3b8"}}>{r.pct>=100?fmtD(r.aktualSelesai):"–"}</b></div>\n                    </div>\n\n                    <div style={{display:"flex",flexDirection:"column",gap:6}}>\n                      {workers.length===0&&(\n                        <div style={{fontSize:11,color:"#94a3b8",fontStyle:"italic",padding:"6px 0"}}>Belum ada operator - klik "Pilih Operator" di atas.</div>\n                      )}\n                      {workers.length>0&&(()=>{\n                        const timerKeys=workers.map((w:any)=>`${r.panelId}_${r.kode}_${proses}_${w.id}`);\n                        const anyTimerRunning=timerKeys.some((k:string)=>!!timerAktif[k]);\n                        const anyLoading=timerKeys.some((k:string)=>timerLoading===k);\n                        let durasiLabel="";\n                        const runningKey=timerKeys.find((k:string)=>timerAktif[k]);\n                        if(runningKey){\n                          const timer=timerAktif[runningKey];\n                          const menitBerjalan=(Date.now()-new Date(timer.mulai).getTime())/60000;\n                          const totalMenit=(timerDurasiSelesai[runningKey]||0)+menitBerjalan;\n                          const jam=Math.floor(totalMenit/60);\n                          const menit=Math.round(totalMenit%60);\n                          durasiLabel=jam>0?`${jam}j ${menit}m`:`${menit}m`;\n                        }\n                        return(\n                          <div style={{display:"flex",flexDirection:"column",gap:6}}>\n                            <div style={{display:"flex",flexWrap:"wrap",gap:6}}>\n                              {workers.map((w:any)=>(\n                                <span key={w.id} style={{display:"inline-flex",alignItems:"center",gap:4,fontSize:11,fontWeight:700,\n                                  color:DIVISI_CONFIG[w.divisi]?.color||"#64748b",background:DIVISI_CONFIG[w.divisi]?.bg||"#f1f5f9",\n                                  borderRadius:20,padding:"4px 10px"}}>\n                                  {DIVISI_CONFIG[w.divisi]?.icon} {w.nama}\n                                </span>\n                              ))}\n                            </div>\n                            <button disabled={anyLoading}\n                              onClick={()=>{\n                                if(anyTimerRunning){\n                                  workers.forEach((w:any)=>{\n                                    const k=`${r.panelId}_${r.kode}_${proses}_${w.id}`;\n                                    if(timerAktif[k])stopTimer(w.id,r.panelId,r.kode,proses);\n                                  });\n                                } else {\n                                  workers.forEach((w:any)=>startTimer(w.id,r.panelId,r.kode,proses,viewDate));\n                                }\n                              }}\n                              style={{fontSize:12,fontWeight:700,border:"none",borderRadius:10,padding:"8px 10px",cursor:anyLoading?"not-allowed":"pointer",\n                                background:anyTimerRunning?"#fef2f2":"#f0fdf4",color:anyTimerRunning?"#dc2626":"#16a34a"}}>\n                              {anyLoading?"...":anyTimerRunning?`⏹ Selesai ${durasiLabel}`:"▶ Mulai"}\n                            </button>\n                          </div>\n                        );\n                      })()}\n                      <button disabled={r.pct===0} onClick={()=>lockSingleKomponen(r.panelId,r.kode,proses)}\n                        style={{fontSize:11,fontWeight:700,border:"none",borderRadius:10,padding:"7px 10px",\n                          cursor:r.pct===0?"not-allowed":"pointer",\n                          background:"#eff6ff",color:"#1d4ed8"}}>\n                        💾 Simpan Progress\n                      </button>\n                    </div>\n'
    new = '                    <div style={{display:"flex",flexDirection:"column",gap:6}}>\n                      {workers.length===0&&(\n                        <div style={{fontSize:11,color:"#94a3b8",fontStyle:"italic",padding:"6px 0"}}>Belum ada operator - klik "Pilih Operator" di atas.</div>\n                      )}\n                      {workers.length>0&&(()=>{\n                        const timerKeys=workers.map((w:any)=>`${r.panelId}_${r.kode}_${proses}_${w.id}`);\n                        const anyTimerRunning=timerKeys.some((k:string)=>!!timerAktif[k]);\n                        const anyLoading=timerKeys.some((k:string)=>timerLoading===k);\n                        let durasiLabel="";\n                        const runningKey=timerKeys.find((k:string)=>timerAktif[k]);\n                        if(runningKey){\n                          const timer=timerAktif[runningKey];\n                          const menitBerjalan=(Date.now()-new Date(timer.mulai).getTime())/60000;\n                          const totalMenit=(timerDurasiSelesai[runningKey]||0)+menitBerjalan;\n                          const jam=Math.floor(totalMenit/60);\n                          const menit=Math.round(totalMenit%60);\n                          durasiLabel=jam>0?`${jam}j ${menit}m`:`${menit}m`;\n                        }\n                        return(\n                          <div style={{display:"flex",flexDirection:"column",gap:6}}>\n                            <div style={{display:"flex",flexWrap:"wrap",gap:6}}>\n                              {workers.map((w:any)=>(\n                                <span key={w.id} style={{display:"inline-flex",alignItems:"center",gap:4,fontSize:11,fontWeight:700,\n                                  color:DIVISI_CONFIG[w.divisi]?.color||"#64748b",background:DIVISI_CONFIG[w.divisi]?.bg||"#f1f5f9",\n                                  borderRadius:20,padding:"4px 10px"}}>\n                                  {DIVISI_CONFIG[w.divisi]?.icon} {w.nama}\n                                </span>\n                              ))}\n                            </div>\n                            <button disabled={anyLoading}\n                              onClick={()=>{\n                                if(anyTimerRunning){\n                                  workers.forEach((w:any)=>{\n                                    const k=`${r.panelId}_${r.kode}_${proses}_${w.id}`;\n                                    if(timerAktif[k])stopTimer(w.id,r.panelId,r.kode,proses);\n                                  });\n                                } else {\n                                  workers.forEach((w:any)=>startTimer(w.id,r.panelId,r.kode,proses,viewDate));\n                                }\n                              }}\n                              style={{fontSize:12,fontWeight:700,border:"none",borderRadius:10,padding:"8px 10px",cursor:anyLoading?"not-allowed":"pointer",\n                                background:anyTimerRunning?"#fef2f2":"#f0fdf4",color:anyTimerRunning?"#dc2626":"#16a34a"}}>\n                              {anyLoading?"...":anyTimerRunning?`⏹ Selesai ${durasiLabel}`:"▶ Mulai"}\n                            </button>\n                          </div>\n                        );\n                      })()}\n                      <button disabled={r.pct===0} onClick={()=>lockSingleKomponen(r.panelId,r.kode,proses)}\n                        style={{fontSize:11,fontWeight:700,border:"none",borderRadius:10,padding:"7px 10px",\n                          cursor:r.pct===0?"not-allowed":"pointer",\n                          background:"#eff6ff",color:"#1d4ed8"}}>\n                        💾 Simpan Progress\n                      </button>\n                    </div>\n\n                    <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",gap:8}}>\n                      <div style={{display:"flex",flexDirection:"column",gap:3}}>\n                        <div style={{display:"flex",alignItems:"center",gap:6,flexWrap:"wrap"}}>\n                          {r.wpDef&&<span style={{background:r.wpDef.color+"18",color:r.wpDef.color,border:`1px solid ${r.wpDef.color}33`,borderRadius:4,padding:"1px 6px",fontSize:10,fontWeight:700}}>{r.wpDef.wp}</span>}\n                          <span style={{fontWeight:700,fontSize:13,color:"#374151"}}>{r.item.nama}</span>\n                        </div>\n                        <div style={{fontSize:10,color:"#94a3b8"}}>{r.task.proyek} · {r.panel.nama}</div>\n                        <div style={{display:"flex",alignItems:"center",gap:6,flexWrap:"wrap"}}>\n                          <span style={{fontSize:10,color:"#94a3b8",fontFamily:"\'DM Mono\',monospace"}}>{r.kode}</span>\n                          <Badge label={r.task.prioritas||"Sedang"} color={r.priColor}/>\n                          {cardMode===\'timer\'&&r.wiringBadge&&(\n                            <span style={{background:"#eef2ff",color:"#4f46e5",borderRadius:6,padding:"1px 6px",fontSize:9,fontWeight:700}}>\n                              ⚡ {(r.wiringBadge.bobot||"").replace("_"," ")} · {r.wiringBadge.jumlahOrang||"–"}org\n                            </span>\n                          )}\n                        </div>\n                      </div>\n                      {r.pct===100\n                        ?<Badge label="TERCAPAI" color="#16a34a" bg="#dcfce7"/>\n                        :r.pct===0\n                        ?<Badge label="BELUM MULAI" color="#94a3b8" bg="#f1f5f9"/>\n                        :<Badge label="ON PROGRESS" color="#2563eb" bg="#dbeafe"/>\n                      }\n                    </div>\n\n                    <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:5,fontSize:10,color:"#64748b"}}>\n                      <div>QTY KOMP: <b style={{color:"#475569"}}>{r.qtyKomp} 🔒</b></div>\n                      <div>CREATE BY: <b style={{color:"#475569"}}>{kInfo.createdBy||"–"}</b></div>\n                      <div>TARGET: <b style={{color:"#1d4ed8"}}>{fmtD(kInfo.targetSelesai)}</b></div>\n                      <div>AKTUAL: <b style={{color:r.pct>=100?"#16a34a":"#94a3b8"}}>{r.pct>=100?fmtD(r.aktualSelesai):"–"}</b></div>\n                    </div>\n'

    count = content.count(old)
    if count == 0:
        fail("Tidak ketemu blok kartu persis seperti yang diharapkan. Struktur mungkin sudah berubah - kirim ulang dump terbaru.")
    if count > 1:
        fail(f"Blok itu muncul {count} kali (harusnya cuma 1) - perlu dicek manual dulu.")

    new_content = content.replace(old, new)

    ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
    backup_path = FILE_PATH + f".bak_{ts}"
    shutil.copy2(FILE_PATH, backup_path)
    print(f"✅ Backup dibuat: {backup_path}")

    with open(FILE_PATH, "w", encoding="utf-8") as f:
        f.write(new_content)

    print("✅ Berhasil diurutkan ulang: Mulai + Simpan Progress sekarang PALING ATAS di tiap kartu.")
    print("\nLangkah selanjutnya:")
    print("   npm run build")

if __name__ == "__main__":
    main()
